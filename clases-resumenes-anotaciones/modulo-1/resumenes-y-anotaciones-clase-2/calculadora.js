// Esta calculadora solo se utilizara si
// mi programa esta en modo "calculos"

export default class calculadora{
    sumar = (num1, num2) => num1 + num2;
    restar = (num1, num2) => num1 - num2;
};